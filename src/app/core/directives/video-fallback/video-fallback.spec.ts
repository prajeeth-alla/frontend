import { VideoFallback } from './video-fallback';

describe('VideoFallback', () => {
  it('should create an instance', () => {
    const directive = new VideoFallback();
    expect(directive).toBeTruthy();
  });
});
